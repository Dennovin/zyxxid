server {
    root /data/www;

    location / {
        try_files $uri $uri/index.html @uwsgi;
    }

    location @uwsgi {
        include uwsgi_params;
        uwsgi_pass unix:///tmp/uwsgi.sock;
    }
}
